# 릴리스 산출물이 기기별 줄바꿈으로 갈리지 않게 저장소는 LF 로 통일한다.
# (볼트 사본 간 3,635 vs 3,537 바이트 차이가 전부 CRLF/LF 때문이었다)
* text=auto eol=lf

# 공유 파일(tasks-gcal-sync 에서 그대로 복사) — peer 비교가 줄끝으로 흔들리지 않게 못 박는다
src/shared/** text eol=lf
tests/*.shared.test.ts text eol=lf
scripts/check-shared.mjs text eol=lf
shared.lock.json text eol=lf
